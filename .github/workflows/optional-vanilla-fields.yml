name: Optional Vanilla Fields

on:
  push:
  pull_request:

jobs:
  optional-vanilla-fields:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Test removed-field compatibility
        run: python3 -m unittest tools.cbg.adapters.cbp.tests.test_optional_vanilla_fields
