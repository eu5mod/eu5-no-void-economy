# TECH-01 — Engine Exposure Matrix

## Purpose

This document tracks whether ModeU5 can rely on a vanilla EU5 scope, scope link, value, trigger, effect, modifier, on_action, or static file field.

No implementation depending on vanilla exposure should proceed until the relevant entry is marked:

```txt
CONFIRMED
FALLBACK_ACCEPTED
OUT_OF_SCOPE
```

## Status values

| Status | Meaning |
|---|---|
| TO_TEST | Need identified, not verified yet. |
| CONFIRMED | Verified in wiki, script docs, vanilla files, or local test. |
| NOT_CONFIRMED | Searched but not found. |
| FALLBACK_ACCEPTED | Not exposed or unreliable, but one fallback has been accepted. |
| OUT_OF_SCOPE | Not required for MVP. |

## Required sources to check

```txt
https://eu5.paradoxwikis.com/Scope_link
https://eu5.paradoxwikis.com/Variable
https://eu5.paradoxwikis.com/Trigger
https://eu5.paradoxwikis.com/Effect
https://eu5.paradoxwikis.com/Modifier_types
https://eu5.paradoxwikis.com/Building_modding
https://eu5.paradoxwikis.com/Goods_modding
https://eu5.paradoxwikis.com/On_actions
local vanilla files
local script_docs output
error.log after test
```

## Wiki audit — 2026-06-13

The wiki pages listed above were reviewed for every matrix entry that was still marked `TO_CHECK` or `TO_TEST`.

- `CONFIRMED` means the required exposure and the exact method named in the row are documented by at least one of those pages.
- `NOT_CONFIRMED` means the exact required exposure was not documented by those pages. Adjacent or partial APIs are recorded in the notes but do not satisfy the row.
- Wiki confirmation does not replace a local script-docs check and controlled `error.log` test before gameplay implementation.
- A wiki `NOT_CONFIRMED` result is not proof that the engine lacks the exposure; it remains blocked until local vanilla files, local script docs, or a controlled test confirm it, or one fallback is accepted.
- Rows 083-085 were added as explicit local-file audits after the wiki pass and remain `TO_TEST` until those files are inspected.
- Row 088 distinguishes ModeU5 logical records from EU5 native variable-map entries. The official 1.1 variable-map documentation defines one key mapped to one value, not an inline multi-field or nested-map value.
- Row 044 combines the ModeU5 direct-formula contract with an engine hook that remains `TO_TEST`.
- Row 081 is a ModeU5 scope contract; its `CONFIRMED` status does not claim an undocumented vanilla income-recipient endpoint.

## Matrix

| ID | US | Need | Expected scope | Candidate exposure | Type | Source checked | Status | Fallback decision | Test / notes |
|---:|---|---|---|---|---|---|---|---|---|
| 001 | Core | Iterate all countries | none → country | `every_country` | effect iterator | Wiki: Effect | CONFIRMED | N/A | Exact syntax: `every_country = { limit = { ... } ... }`. |
| 002 | Core | Iterate all markets | none → market | `every_market_in_world` | effect iterator | Wiki: Effect | CONFIRMED | N/A | The documented name is `every_market_in_world`, not `every_market`. Needed for annual validation/rebuild. |
| 003 | Core | Iterate owned locations of a country | country → location | `every_owned_location` | effect iterator | Wiki: Effect | CONFIRMED | N/A | Exact iterator is documented on country scope. |
| 004 | Core | Identify a location's market | location → market | `market` | scope link | Wiki: Scope link | CONFIRMED | N/A | `market` links a location to its market. |
| 005 | Core | Identify location owner | location → country | `owner` | scope link | Wiki: Scope link | CONFIRMED | N/A | `owner` accepts location scope and returns country. |
| 006 | Core | Iterate or identify goods | none → goods | `every_goods` | effect iterator | Wiki: Effect; Goods modding | CONFIRMED | N/A | `every_goods` iterates all goods types; static goods are in `in_game/common/goods`. |
| 007 | Core | Store one durable field indexed by a scoped key | variable-capable scoped owner or global map plus one scoped key | `add_to_variable_map`, <code>variable_map(name&#124;key)</code>, `remove_from_variable_map`, `add_to_global_variable_map`, <code>global_variable_map(name&#124;key)</code>, `remove_from_global_variable_map` | variable map | Wiki: Variable 1.1 variable maps; Effect; local vanilla HRE scripts | CONFIRMED | N/A | A native map entry is one key mapped to one number or scope. Follow `VARIABLE_MAP_STORAGE_MODEL.md`: represent a multi-field logical record through a synchronized map family until 088 is resolved. Map names are static; update existing keys by remove/re-add. A scope must support variables; Market scope does not in the tested EU5 build. |
| 008 | Core | Save and reuse country/market/good scopes in effects | current effect scope | `save_scope_as`, `save_temporary_scope_as`, `clear_saved_scope` | scope handling | Wiki: Effect; Variable | CONFIRMED | N/A | `save_scope_as` survives an unbroken event chain; temporary scopes last for the current effect. |
| 009 | Core | Apply temporary country modifier | country | `add_country_modifier` | effect | Wiki: Effect | CONFIRMED | N/A | Supports duration, mode, dynamic `size`, description localization, and immediate recalculation. |
| 010 | Core | Apply or replace a temporary location modifier | location | `add_location_modifier`, `remove_location_modifier` | effect | Wiki: Effect | CONFIRMED | N/A | `add_location_modifier` supports days/months/years, mode, dynamic `size`, description, and `recalculate_immediately`. Use a one-month duration or explicit remove/replace in the shared monthly cycle; verify replacement locally. |
| 011 | Core | Monthly country cycle | none → country | `monthly_country_pulse` | on_action | Wiki: On actions | CONFIRMED | N/A | The pulse changes scope to each country and fires its `pulse_effect`. ModeU5 should register one shared monthly dispatcher and preserve the normative runtime order. |
| 012 | Core | Yearly country cycle | none → country | `yearly_country_pulse` | on_action | Wiki: On actions | CONFIRMED | N/A | The documented yearly pulse changes scope to each country. ModeU5 should register one shared yearly dispatcher. |
| 013 | Core | Fire debug/test events | effect scope | `trigger_event_silently`, `trigger_event_non_silently`, `debug_log` | event effect | Wiki: Effect; controlled CORE-01 event/log tests on June 15, 2026; controlled US-01/US-02 console logging assertions on June 16, 2026 | CONFIRMED | N/A | Supports event IDs, named on_actions, optional day/month/year delay, and `debug_log` trace lines in `debug.log`. A called result event must not be declared `orphan = yes`. `error_log` is reserved for failed assertions or unexpected anomalies; expected business-rule rejection is reported through debug/result state. For console-triggered deterministic tests, do not use `test_log` or `debug_log`: US-02 showed `test_log` localizing with command localization disabled, US-01 showed wrapper-event `debug_log` doing the same, and the later US-01 rerun showed scripted-effect `debug_log` doing the same. Use result markers, result events, and scope debug variables instead. |
| 014 | Core | Localization for modifiers/tooltips | effect/modifier/static object | `custom_tooltip`, modifier `desc`, object `<key>` / `<key>_desc`; `main_menu/localization/english/*.yml` | localization hook / data path | Wiki: Effect; Building modding; Goods modding; local vanilla files | CONFIRMED | N/A | Localization-key hooks and the loaded English localization path are confirmed. UTF-8 BOM files use the `l_english:` header. |
| 015 | CORE-01 / US-01 | Current country stock in market for good | country × market × good logical record | logical `stock` field; physical country-scoped `modeu5_<good>_stock_by_market` map keyed by market | internal record field / variable map | ModeU5; TECH-01 007 | CONFIRMED | N/A | Logical name remains `country_market_good_stock`. Only centralized stock effects may write the physical field map. Missing entries default explicitly to zero. |
| 016 | CORE-01 / US-01 | Market stock for good | market × good | global per-good `modeu5_<good>_market_stock` map keyed by market scope | internal global variable map | ModeU5; TECH-01 007; local vanilla HRE global-map scripts; controlled test and logs on June 14, 2026 | FALLBACK_ACCEPTED | Use a global per-good map keyed by market | Logical name remains `market_good_stock`. Controlled runtime logs confirmed that Market scope does not support variables, so the rejected market-owned map is replaced by this stable aggregate/cache. Rebuild only from country stock maps. |
| 017 | CORE-01.1 / CORE-01.3 / US-01 | Country stock capacity | country × market × good logical record | logical `capacity` field; physical country-scoped `modeu5_<good>_stock_cap_by_market` map keyed by market | internal record field / variable map | ModeU5; TECH-01 007 | CONFIRMED | N/A | Logical name remains `country_market_good_stock_cap`. Missing entries default explicitly to zero. |
| 018 | CORE-01.1 / CORE-01.3 / US-01 | Available stock capacity | ModeU5 | `max(0, stock_cap - stock)` | internal scripted value | ModeU5 | CONFIRMED | N/A | Defined by the mod and enforced by default. CORE-02/CORE-03 may explicitly use TECH-01 099 `allow_over_capacity`. |
| 019 | CORE-01.5 / US-11 | Rebuild market stock from country stocks | ModeU5 | modeu5_rebuild_market_stock_from_country_stocks | internal effect | ModeU5 | CONFIRMED | N/A | Must not modify country stocks. |
| 020 | CORE-01.6 / US-11 | Validate stock consistency | ModeU5 | modeu5_validate_stock_consistency | internal effect | ModeU5 | CONFIRMED | N/A | Corrects market aggregate only by delegating to 019. |
| 021 | US-00.1 | Read location production by good and aggregate it into the country ledger | current country → owned location × good → location market | location `goods_output` for a target good; location `raw_material_output` when `raw_material = good`; validation against `produced_in_country:<good>` / `produced_in_market:<good>` | value / aggregation design | Wiki: Trigger; local vanilla trigger localization and scripts | TO_TEST | N/A | The required data exists. Sum each owned location's output into `country × location.market × good`; source-level building/RGO output is not required. Controlled script test must confirm the exact target-good syntax for `goods_output` and treatment of foreign-owned buildings before gameplay implementation. |
| 022 | CORE-01.1 / US-00.1 | Actual quantity added to stock | ModeU5 | modeu5_add_stock output | internal variable | ModeU5 | CONFIRMED | N/A | Defined by mod. |
| 023 | CORE-01.1 / US-00.1 | Rejected quantity | ModeU5 | quantity_to_add - actual_added_quantity | internal variable | ModeU5 | CONFIRMED | N/A | Defined by mod. |
| 024 | US-00.1 | Ledger helper | ModeU5 | modeu5_update_production_rejection_ledger | internal effect | ModeU5 | CONFIRMED | N/A | Must be used for all ledger writes. |
| 025 | US-00.1 | Store and read one monthly production-ledger record keyed by country × market × good | logical fields `produced`, `added`, `rejected`; physical synchronized country-scoped per-good maps keyed by market | `modeu5_<good>_produced_by_market`, `modeu5_<good>_added_by_market`, `modeu5_<good>_rejected_by_market` plus map add/read/remove/clear operations | logical record / variable-map family | Wiki: Variable; Effect; TECH-01 007 | CONFIRMED | N/A | One centralized helper owns the three fields. Existing physical keys require remove/re-add; clear only after all monthly consumers run. Do not reconstruct the record from final stock snapshots. |
| 026 | US-00.2 | Calculate ratios with division/min/max | scripted effect/value | `change_variable` operations `divide`, `min`, `max` | scripted math | Wiki: Variable; Effect | CONFIRMED | N/A | `change_variable` documents add/subtract/multiply/divide/modulo/min/max. Script must guard `produced <= 0` before division. |
| 027 | US-00.3 | Good-specific local output modifier | location, one generated modifier per good | `local_<good>_output_modifier` | modifier | Wiki: Goods modding; Modifier types | CONFIRMED | N/A | Goods modding defines the generated pattern; concrete entries such as `local_iron_output_modifier` are listed. Apply through 010. |
| 028 | US-00.3 | Local production efficiency modifier | location | `local_production_efficiency` | modifier | Wiki: Modifier types | CONFIRMED | N/A | Exact location modifier, percent-good format. Apply through 010; it affects all local goods. |
| 029 | US-00.3 | Identify affected production sources/locations producing the good | building/location/production method/goods | `every_buildings_in_location`, `building_produced_goods`, `every_production_method_of_building`, `produced_goods`, location `raw_material` | iterator/trigger/scope link | Wiki: Effect; Trigger; Scope link | CONFIRMED | N/A | These APIs identify producing buildings, production methods, and RGO goods. Use the country-rooted attribution rule in 081. |
| 030 | US-00.4 | Good price for void wealth | market × good | `market_price`; fallback `default_price` / `default_market_price` | value/static field | Wiki: Scope link; Trigger; Goods modding | CONFIRMED | N/A | `market_price` is exposed on market for scoped goods; `default_market_price` is the static goods field. Log `good_price_source`. |
| 031 | US-00.4 | Estate tax base for proxy sizing | country or estate | `estate_tax_base` | value/trigger | Wiki: Scope link; Trigger | CONFIRMED | N/A | Correct documented name is `estate_tax_base`, not `estate_taxable_income`. Optional proxy only; not the primary punishment. |
| 032 | US-00.4 | Estate tax percentage for proxy sizing | country | `estate_tax_percentage` | value/trigger | Wiki: Scope link; Trigger | CONFIRMED | N/A | Correct documented name is `estate_tax_percentage`, not `estate_tax`. |
| 033 | US-02 | Count owned locations in market | country → location → market | `every_owned_location` plus location `market` | iterator/scope link | Wiki: Effect; Scope link | CONFIRMED | N/A | Iterate country-owned locations and retain those whose `market` equals the target market. |
| 034 | US-02 | Buildings in location | location → building | `every_buildings_in_location` | effect iterator | Wiki: Effect | CONFIRMED | N/A | Exact iterator is documented. |
| 035 | US-02 | Foreign buildings in location | location → building | `every_foreign_buildings_in_location` | effect iterator | Wiki: Effect; Building modding | CONFIRMED | N/A | Building modding also documents `is_foreign = yes` and distinguishes building owner from location owner. |
| 036 | US-03 | Invoke monthly decay in the shared cycle | country | `monthly_country_pulse` → ModeU5 monthly dispatcher → `modeu5_decay_stock` | on_action integration | Wiki: On actions; ModeU5 runtime order; TECH-01 011 | CONFIRMED | N/A | This is an orchestration rule, not a separate engine pulse. Invoke decay at runtime step 12 and do not register a second monthly mechanism. |
| 037 | US-04 | Detect an unmet Pop-relevant need for a good | market × good, attributed to country × market × good by ModeU5 | Candidate shortage trigger comparing Pop-relevant demand with supply plus stockpile | trigger / shortage signal | Local vanilla `pop_demand` file; local script docs pending; controlled test pending | NOT_CONFIRMED | Use the US-10.3 unsatisfied Pop-demand outcome as the ModeU5 adaptation signal when available | This is a downstream unmet-need signal for US-04, not the upstream requested-demand quantity used by US-10.1. |
| 038 | US-04 | Population by type in location | location | `num_pop_type`, `percentage_pop_type_in_location` | value/trigger | Wiki: Scope link; Trigger | CONFIRMED | N/A | Both exact location values are documented. |
| 039 | US-04 | Apply a local demand modifier to vanilla demand | location × good × Pop context | local dynamic Pop-demand modifier/effect | modifier/effect | Wiki: Goods modding; Modifier types; Effect | CONFIRMED | Simulated demand only | Static `pop_demand` script values and country-wide `global_<good>_pop_demand` modifiers exist, but no local location × good runtime modifier/application effect is documented. |
| 040 | US-04 | Track yearly satisfied/unsatisfied months | location × good logical demand record | logical counter fields; physical location-scoped `modeu5_pop_demand_satisfied_months` and `modeu5_pop_demand_unsatisfied_months` maps keyed by goods scope | internal record fields / variable-map family | ModeU5; TECH-01 007 | CONFIRMED | N/A | Missing entries default to zero. Read, adjust, remove, and re-add each physical key; reset only after US-04 yearly adaptation reads the record. |
| 041 | US-05 | Read the vanilla Stability/Court slider cost | country/slider | readable slider cost value | value | ModeU5 direct-formula design | OUT_OF_SCOPE | N/A | Not required when ModeU5 replaces the Economic Base formula directly. Do not implement monthly slider reconciliation in this design. |
| 042 | US-05 | Monthly trade income | country | `monthly_trade_income` | value/trigger | Wiki: Trigger | CONFIRMED | N/A | Exact country value/trigger is documented. `monthly_income_trade_and_tax` is a separate broader value. |
| 043 | US-05 | Read total country wealth for the Economic Base formula | country | script value equivalent to GUI `Country.GetTotalWealth`, or a confirmed aggregate | value | Local vanilla GUI; Wiki: Trigger; Scope link | TO_TEST | One accepted ModeU5 wealth aggregate if no script value exists | Vanilla GUI exposes `Country.GetTotalWealth`, proving the engine has the value, but the exact gameplay-script value is not documented. Test a script equivalent before accepting reconstruction. |
| 044 | US-05 | Replace the Economic Base formula used by Stability and legitimacy-producing Court costs | economy/slider formula context | modifiable formula or controlled call site using `modeu5_slider_cost_base = Wealth + monthly_trade_income` | formula/static/script hook | Local vanilla economy GUI and common files; ModeU5 specification | TO_TEST | No reconciliation fallback in the selected design | The formula contract is clear, but local files have not yet exposed a moddable Stability/Court base-formula hook. Confirm the actual call site before implementation. |
| 045 | US-05 | Apply visible slider reconciliation | country/UI | sized modifier or gold effect with visible presentation | effect/modifier/UI | ModeU5 direct-formula design | OUT_OF_SCOPE | N/A | Reconciliation is not part of the selected direct-formula design. US-05-UI must explain the replacement formula and inputs instead. |
| 047 | US-10.2 | Iterate vanilla trades | country/market → trade | `every_trade`, `ordered_trade` | effect iterator | Wiki: Effect | CONFIRMED | N/A | Both trade iterators are documented; ordered iteration supports deterministic filtering and ordering. |
| 048 | US-10.2 | Iterate vanilla imports | country/market → import | `every_import`, `ordered_import` | effect iterator | Wiki: Effect | CONFIRMED | N/A | Both import iterators are documented. |
| 049 | US-10.2 | Iterate vanilla exports | country/market → export | `every_export`, `ordered_export` | effect iterator | Wiki: Effect | CONFIRMED | N/A | Both export iterators are documented. |
| 054 | US-10.2 | Identify source and target markets of a trade | trade → market | `from_market`, `to_market` | scope link | Wiki: Scope link | CONFIRMED | N/A | Both links are documented on trade scope. |
| 055 | US-10.2 | Identify the traded good | trade → goods | `traded_goods` | scope link | Wiki: Scope link | CONFIRMED | N/A | The trade-to-goods link is documented. |
| 056 | US-10.2 | Read actual and desired quantity while iterating a vanilla trade | trade | script value equivalent to GUI `Trade.GetQuantityOfGoodsActuallyMoved`; optional desired quantity equivalent to `Trade.GetDesiredGoodsToShip` / `GetDesiredGoodsShipped` | value | Local vanilla GUI; Wiki: Trigger; Scope link | TO_TEST | Use an explicit requested quantity for ModeU5-created transfers; do not infer a vanilla request | Trade iteration enters trade scope and the engine exposes exact quantities to GUI, but no gameplay-script trigger/value name is documented. A controlled script test must prove the equivalent value is callable inside `every_trade`/`ordered_trade`. |
| 063 | US-07 | Trade building modifier names | building/location modifier block | `local_burghers_estate_power`, `local_merchant_power` | static modifier | Wiki: Modifier types; Building modding | CONFIRMED | N/A | Both exact modifier names are documented; building `modifier` applies regular location modifiers. Local vanilla entries still determine which buildings to override. |
| 064 | US-08 | Goods price field | `in_game/common/goods` | `default_market_price` | static field | Wiki: Goods modding | CONFIRMED | N/A | The documented field is `default_market_price`, not `base_price`. |
| 065 | US-08 | Building dynamic price rules | building type static definition | `price`, `p_building_<key>`, `p_expensive_building_<key>`, `expensive`, `increase_per_level_cost` | static field/rule | Wiki: Building modding; Scope link | CONFIRMED | N/A | The `<key>` is the building's unlock age, or the first age when no advance unlocks it. `building_base_cost_in_gold` is also exposed. RGO pricing is tracked separately in 082. |
| 066 | US-09 | Production efficiency bonus | country | `global_production_efficiency` | modifier | Wiki: Modifier types | CONFIRMED | N/A | Exact country modifier, percent-good format. Apply with `add_country_modifier` from 009. |
| 067 | US-10.0 | Opinion buyer toward seller | country → country | `opinion = { target = <country> value ... }` | value/trigger | Wiki: Trigger; Variable | CONFIRMED | N/A | Exact target-country comparison/value is documented. |
| 068 | US-10.0 | Trade advantage in market | market with country argument/context | `merchant_power_in_market` | value/trigger | Wiki: Trigger; Variable | CONFIRMED | N/A | Returns market merchant power for a country in the scoped market. |
| 069 | US-10.0 | Market access | location | `market_access` | value/trigger | Wiki: Trigger; Variable | CONFIRMED | N/A | Exact exposure is location-scoped; country/market aggregation must be scripted. |
| 070 | US-10.0 | Embargo relation | country → country | `is_embargoed_by`, `is_embargoing` | trigger | Wiki: Trigger | CONFIRMED | N/A | Both relation directions are documented. |
| 071 | US-10.0 | War relation | country → country | `is_at_war_with` | trigger | Wiki: Trigger | CONFIRMED | N/A | Exact target-country trigger is documented. |
| 072 | US-10.0 | Subject relation | country → country | `is_subject_of`, `overlord` | trigger/scope link | Wiki: Trigger; Scope link | CONFIRMED | N/A | Both the relation trigger and country-to-overlord link are documented. |
| 073 | US-10.0 | Market owner | market → country | `owner` | scope link | Wiki: Scope link | CONFIRMED | N/A | `owner` accepts market scope and returns country. The alias `market_owner` is not documented. |
| 074 | US-10.0 | Ordered candidate sorting | list/map/typed iterator | `ordered_in_list`, `ordered_key_in_variable_map`, typed `ordered_*` with `order_by` | iterator | Wiki: Variable; Effect | CONFIRMED | N/A | Supports `limit`, `order_by`, position/min/max, and deterministic bounded iteration. |
| 075 | CORE-01.2 / US-10.1 | Resolve consumption without trade | ModeU5 | modeu5_remove_stock | internal effect | ModeU5 | CONFIRMED | N/A | Must never create trade income/cost. |
| 076 | CORE-01.3 / US-10.2 | Resolve inter-market transfer | ModeU5 | modeu5_transfer_stock | internal effect | ModeU5 | CONFIRMED | N/A | US-10.2 applies only when source_market != target_market; the core operator also supports same-market ownership transfer without changing the market aggregate. |
| 077 | US-10.3 | Track satisfied and unsatisfied demand outcomes | logical consumer-target demand record | logical requested/satisfied/unsatisfied fields; physical location-scoped maps keyed by goods for Pops and country-scoped per-good maps keyed by market for broader aggregates | internal record / variable-map family | ModeU5; TECH-01 007 | CONFIRMED | N/A | One request's arithmetic stays local. Persist only monthly/yearly record fields. Pop outcome fields feed US-04 and share its location × good tuple contract. |
| 078 | US-13 | Non-horde check | country | `government_type = government_type:steppe_horde`; invert for non-horde | trigger / scope comparison | Wiki: Scope link; Government; local vanilla events and auto modifiers | CONFIRMED | N/A | Vanilla repeatedly uses this exact country trigger, including `events/government/steppe_horde.txt` and `events/disaster/horde_civil_war.txt`. |
| 079 | US-13 | Current age | current script context | `current_age` | trigger | Wiki: Trigger | CONFIRMED | N/A | Exact trigger is documented; accepted values still require local vanilla/static-file inspection. |
| 080 | US-13 | Conquest CB/wargoal cost override | static CB/wargoal files | `conquer_cost` | static field | Wiki pages | CONFIRMED | no implementation until confirmed | No `conquer_cost` field or equivalent conquest-cost override is documented on the reviewed pages. |
| 081 | US-00.1 | Establish the ModeU5 ledger country for location aggregation | country-rooted cycle → owned location | current country from `monthly_country_pulse`; `every_owned_location`; location `owner` validation | scope contract / iterator / scope link | Wiki: On actions; Effect; Scope link; local vanilla scripts; ModeU5 | CONFIRMED | N/A | The ledger country is the current country running the monthly cycle. Aggregate its owned locations by `location.market`; do not require an undocumented vanilla income-recipient concept. Foreign-building treatment is tested under 021. |
| 082 | US-08 | RGO dynamic price rules | RGO/static economy files | RGO construction/upgrade price fields | static field/rule | Wiki pages | NOT_CONFIRMED | do not override until confirmed | Goods modding documents `block_rgo_upgrade`, AI RGO fields, and upgrade-demand names, but not RGO price calculation or an RGO base-price field. |
| 083 | US-07 | Identify exact vanilla trade-building definitions and current values | local vanilla building files | in-scope building keys, modifiers, costs, and capacities | static data audit | Local vanilla files pending | CONFIRMED | N/A | Confirm the exact entries and source values before creating overrides or claiming tooltip differences. |
| 084 | US-08 | Identify the exact in-scope building and RGO definitions | local vanilla building/RGO files | definition keys and current price-related values | static data audit | Local vanilla files pending | TO_TEST | N/A | This inventory is separate from whether the RGO price hook itself exists under 082. |
| 085 | US-08-UI | Bind native building/RGO price breakdowns into UI | vanilla GUI and localization files | existing price tooltip/datacontext binding | UI exposure | Local vanilla GUI files pending | TO_TEST | Documentation/debug only | Do not claim the native tooltip can show the ModeU5 breakdown until the binding is confirmed. |
| 086 | US-10.1 | Read Estate and other non-Pop consumer demand context | estate/country/consumer caller | reliable vanilla requested-quantity inputs | value/caller context | Wiki pages; local script check pending | NOT_CONFIRMED | One explicitly accepted simulated-demand caller | No reliable generic Estate/other consumer requested-quantity input has been identified. |
| 087 | US-10.1 | Read runtime requested Pop demand quantity by good | Pop / location × good | reliable vanilla requested-quantity input passed to `modeu5_resolve_stock_demand` | value / caller context | Local `pop_demand` files; local script docs pending; controlled test pending | NOT_CONFIRMED | One explicitly accepted simulated-demand source | This is the upstream requested quantity for stock resolution; do not confuse it with exposure 037 or the US-10.3 unsatisfied outcome. |
| 088 | Core | Store one multi-field record or nested map as a single variable-map value | variable-map entry / unique persistent record scope | inline structured value, nested variable map, or a unique persistent scope per ModeU5 tuple that owns named variables | storage abstraction | Wiki: Variable 1.1 variable maps; local script docs pending | NOT_CONFIRMED | Use one logical record backed by a synchronized family of confirmed single-value maps | Official documentation defines one map key mapped to one scope or number. It does not document inline fields or a nested map value. A scope-valued entry would still require a unique persistent record scope for every tuple; none is currently identified. No MVP behavior is blocked by using the map-family representation. |
| 089 | CORE-02 | Run one delayed start-game initializer | none | extend `on_game_start` through `on_actions`, with `delay = { days = 1 }` before the ModeU5 dispatcher | on-action integration | Wiki: On actions | CONFIRMED | N/A | The wiki explicitly warns that nations may not be initialized during immediate `on_game_start` and documents the one-day-delay pattern. Add only an `on_actions` block to the hardcoded hook. |
| 090 | CORE-02 | Persist and test initialization/schema state without a root scope | none | persistent global variables, `global_var:`, `has_global_variable`, global variable effects | variable/effect/trigger | Wiki: Variable; Effect; Trigger | CONFIRMED | N/A | Use a schema version and initialization state so startup is idempotent and never refills an initialized campaign. |
| 091 | CORE-02 | Read the opening vanilla quantity for one market and good | market × good | `"stockpile_in_market(goods:<good>)"` after the delayed hook | value trigger | Wiki: Trigger; vanilla `black_death.txt`, `generic_progress_and_literacy_mission_pack.txt`, and `02_country_specific.txt`; controlled probe on June 15, 2026 | CONFIRMED | N/A | The delayed probe resolved FRA's capital market, persisted the numeric wheat stockpile value, and set its PASS marker without creating ModeU5 stock, schema-version, or initialization-state data. Zero remains a valid result. The rejected `stockpile_in_market:<good>` form must not be restored because EU5 treated the good as an event-target link and reported a missing goods field. |
| 092 | CORE-02 | Calculate a fallback opening quantity without vanilla stock exposure | market × good | synthetic capacity fill ratio | internal scripted value | ModeU5 design review | OUT_OF_SCOPE | N/A | Rejected business rule. Opening stock must come from the current vanilla market stock only. |
| 093 | CORE-02 | Select a deterministic country for positive allocation residue | temporary eligible-country set → country | `ordered_in_list` or typed `ordered_country` with `order_by = country_capacity`, positive-capacity limit, and bounded result | ordered iterator | Wiki: Effect; TECH-01 074 | CONFIRMED | The implementation uses typed `ordered_country` over positive-capacity countries to avoid a persistent helper list. Still call `modeu5_add_stock` with `allow_over_capacity`; do not write residue directly. |
| 094 | CORE-03 | React to a permanent location ownership change | location with loser/winner | `on_location_changed_owner`; root location, `scope:loser`, `scope:winner` | on action | Wiki: On actions | CONFIRMED | N/A | This is an ownership hook, not temporary occupation. Use it as the primary stock-succession transaction. |
| 095 | CORE-03 | Detect a newly formed or released country | new country | `on_new_country_formed`, `on_released_country` | on action | Wiki: On actions | CONFIRMED | N/A | These hooks validate/finalize after location-level transfers; they must not independently repeat the proportional split. |
| 096 | CORE-03 | Finalize stock after annexation or absorption | successor with disappearing target | `on_annexed`, `on_diplomatic_annexed`, `on_military_annexed`, `on_civil_war_annexed` | on action | Wiki: On actions | CONFIRMED | N/A | The documented hooks fire after annexation and expose the annexing country plus target. Use them to consolidate residual target stock. |
| 097 | CORE-03 / US-02 | Calculate capacity carried by one transferred location | location x market x good | same per-location and building contribution helper used by US-02 capacity totals | internal scripted value/effect | ModeU5; TECH-01 033-035 | CONFIRMED | N/A | The numerator must use the exact US-02 capacity logic so sequential stock succession reconciles with capacity recomputation. |
| 098 | CORE-03 | Confirm lifecycle hook coverage and ordering for peace, rebels, vassals, releases, and tag formation | location/country lifecycle | controlled tests of 094-096, including duplicate-hook detection | on-action sequencing | Wiki: On actions; local test pending | TO_TEST | Location-owner-change owns splitting; country hooks only validate/finalize | Do not implement a second split path. Test that each permanent location transfer is processed once and that delayed finalization occurs after the location sequence. |
| 099 | CORE-01.1 / CORE-01.3 / CORE-02 / CORE-03 | Select whether target capacity is enforced for a centralized stock transaction | ModeU5 transaction | `capacity_policy = enforce/allow_over_capacity`; `target_capacity_policy = enforce/allow_over_capacity` | internal scripted-effect contract | ModeU5 business rule | CONFIRMED | N/A | Default to `enforce`. Only CORE-02 initialization, CORE-03 succession, and a separately approved migration may use `allow_over_capacity`. Capacity remains a weight for CORE-02/03, and the full state-derived quantity is conserved. |
| 100 | CORE-00 | Define custom ModeU5 game rules | main menu | `main_menu/common/game_rules/<file>.txt` using the documented `game_rule` schema | static main-menu definition | Local vanilla `_game_rules.info` and `00_game_rules.txt` | CONFIRMED | N/A | Custom rules and settings are data-defined. Package selection remains authoritative for optional static overrides. |
| 101 | CORE-00 / optional scripted stories | Read an active game-rule setting during gameplay | global/script context | `has_game_rule = <setting>` | trigger | Local vanilla scripts and trigger localization | CONFIRMED | N/A | Vanilla uses this trigger in events, scripted triggers, institutions, diseases, and customizable localization. |
| 102 | CORE-00 / US-07 / US-08 | Disable arbitrary static building/RGO numeric overrides through a runtime game rule | static building/RGO definition | game-rule-conditioned `price`, modifier, or equivalent static field | static conditional override | Local `_game_rules.info`, building definitions, and vanilla usage audit | NOT_CONFIRMED | Separate optional companion packages | Game rules expose modifiers, defines, flags, and runtime `has_game_rule` checks. No reviewed example makes arbitrary static numeric fields conditional. Do not ship loaded US-07/08 overrides behind a cosmetic toggle. |
| 103 | CORE-00 | Require matching ModeU5 Core when a companion package is selected | launcher/mod metadata | `.metadata/metadata.json` dependency relationship | package metadata | Controlled launcher dependency tests on June 14, 2026; TECH-01 106 | CONFIRMED | N/A | Selecting a companion automatically selects compatible Core. The launcher does not cascade-disable companions when Core is manually disabled and selecting Core does not auto-select companions; document and validate the playset before campaign load. |
| 104 | CORE-01.1-01.6 | Select one literal per-good persistence adapter | scripted effect call | custom `$argument$` effect-name selection plus generated EU5 adapters containing literal map identifiers | scripted-effect contract | Local vanilla `in_game/common/scripted_effects/readme.txt`; local goods definitions; local `error.log`, `game.log`, and `debug.log` on June 14-15, 2026 | CONFIRMED | N/A | Custom scripted-effect arguments can select the per-good adapter name, but map identifiers cannot safely be passed as arguments to variable-map effects or value expressions. Both nested and direct forwarding produced malformed identifiers such as `modeu5wheat$_stock_by_market` and `variable_mapmodeu5_wheat_stock_by_market$...`. The 07:02:06 and 07:02:16 audits were identical: bare capacity identifiers were recognized literally while quoted map-value expressions were malformed, so this was a syntax-path difference rather than changing runtime state. A literal `used but never set` warning is static-analysis output and does not by itself prove a failed runtime access; any identifier retaining `$` is invalid. Each generated EU5 adapter must therefore contain complete literal stock, capacity, aggregate, rebuild, and validation map reads/writes, then call shared arithmetic effects that receive no map-name parameters. The shell generator only expands the versioned EU5 template and contains no stock business rules. |
| 105 | Core / scripted values | Load reusable scripted numeric values | common database | `in_game/common/script_values/*.txt` | data path / scripted value | Local vanilla directory and files | CONFIRMED | N/A | The loaded EU5 directory is `script_values`, not `scripted_values`. `modeu5_default_monthly_decay_rate` and stock-consistency thresholds are stored under the confirmed path. |
| 106 | CORE-00 | Declare the matching No Void Economy dependency and flag an invalid companion-only playset | launcher/mod metadata | `.metadata/metadata.json` relationship with `rel_type = dependency`, Core `id`, `resource_type = mod`, and compatible `version` | package metadata | Local launcher dependency states; EU5 Community Mod Framework metadata and documentation; controlled launcher test on June 14, 2026 | CONFIRMED | N/A | Each companion declares `modeu5_core` version `0.1.*`. Enabling a companion automatically enables compatible Core. Disabling Core does not cascade-disable already selected companions, and enabling Core does not select optional companions. Packages remain sibling entries. |
| 107 | CORE-00 | Display a package lifecycle warning in the mod manager and selected playset | launcher/mod metadata | `.metadata/metadata.json` `short_description` exposed as `LocalMod.Description` and `ModsPlaysetEntry.Description` | package metadata / launcher UI | Local `main_menu/gui/mods_gui/mods_gui_templates.gui` | CONFIRMED | N/A | Available-mod and selected-playset tooltips render the metadata description. Use it to state that the package set must be selected before campaign start and kept unchanged for that save. This warns but does not block the user. |
| 108 | Core | Clamp scripted numeric values to lower and upper bounds | script value arithmetic | `min = <lower_bound>` and `max = <upper_bound>` | value block operations | Local vanilla `generic_trade_mission_pack.txt`, `disaster_triggers.txt`, and `country_effects.txt`; controlled CORE-01 test on June 15, 2026 | CONFIRMED | N/A | EU5 naming is bound-oriented: `min` enforces the minimum allowed result and `max` enforces the maximum allowed result. Therefore `max(0, value)` is written with `min = 0`, while `min(value, capacity)` is written with `max = capacity`. Reversing these operators silently reduced every positive CORE-01 request to zero without producing a script-system error. |
| 109 | CORE-01.5-01.6 | Accumulate transaction-local values across an iterator | country controller scope | `set_local_variable`, `change_local_variable`, `local_var:<name>` | local-variable effects and value access | Local vanilla `imperial_examination_events.txt`, `qa_debug.txt`, and `wokou_events.txt` | CONFIRMED | N/A | Initialize the accumulator on the calling country scope, update it from `every_country` through the saved controller scope, copy final values into temporary scope values, and never persist transaction accumulators in durable ModeU5 maps. |
| 110 | Core deterministic tests | Read an optional PASS marker without errors when it is absent | global variable system | `has_global_variable`, `NOT = { has_global_variable = ... }` | trigger | Controlled CORE-01 result-event and `error.log` tests on June 15, 2026 | CONFIRMED | N/A | Numeric comparison against an unset global result variable produced missing-variable, unset-scope, and invalid-comparison errors. Test result UI must use marker presence; the marker name is not a console command. |
| 111 | US-11 | Maintain and iterate a deduplicated dirty-market index per good | global variable system → market | `add_to_global_variable_list`, `has_global_variable_list`, `is_target_in_global_variable_list`, `every_in_global_list` with `variable =`, `clear_global_variable_list` | global variable list | Wiki: Variable; Trigger; Effect; controlled US-11 test on June 15, 2026 | CONFIRMED | N/A | Use one literal global variable-list name per good. Add a market only when absent, process each listed market through CORE-01.6, then clear the list. In the tested build, clearing the final entries leaves no readable list: verify emptiness with `NOT = { has_global_variable_list = <name> }`; calling `global_variable_list_size` on the cleared list emits a missing-list script error. The list is a scheduling index and never a stock source. |
| 112 | US-11 | Run one global reconciliation from country pulses at most once per calendar cycle | country pulse → global state | `current_year`, `current_month`, global numeric stamp guarded by `has_global_variable` | trigger/value/global variable | Wiki: Trigger; Effect; TECH-01 011-012 | CONFIRMED | N/A | Calculate `year * 12 + month` for monthly reconciliation and use `year` for yearly reconciliation. Check marker presence before numeric comparison. Automatic reconciliation remains gated by CORE-02 initialization. |
| 113 | CORE-01 / CORE-02 / CORE-03 / US-03 / US-11 | Establish numeric precision, rounding, persistence, and accumulation behavior | script values, temporary values, variable maps, iterator accumulators | controlled fractional arithmetic and map write/read probe | numeric engine behavior | Wiki: Variable, Trigger, Effect; controlled runtime test pending | TO_TEST | Keep current exact accounting and integer-dominant fixtures; preserve raw differences in debug | Official documentation accepts numeric, `float`, and `real` values but does not specify representation, decimal precision, rounding mode, map serialization precision, or approximate equality. Follow `docs/technical/NUMERIC_PRECISION_AND_TEST_DIAGNOSTICS.md`. Do not introduce one shared epsilon into tests, validation, and gameplay guards without separate approval. |
| 114 | US-02 | Iterate compatible foreign buildings owned by one country and attribute their capacity in the building's market | country → building → location → market | `every_owned_foreign_building`, building `location`, location `market`, `building_type`, and `building_level` | effect iterator / scope link / trigger / value | Wiki: Effect, Scope link, Trigger; local vanilla `nanbokuchou.txt`, `flavor_flo.txt`, `country_effects.txt`, and building definitions | CONFIRMED | N/A | Capacity belongs to the foreign building owner. The initial compatible inventory is foreign `fondaco`; `every_foreign_buildings_in_location` remains useful for location-side inspection but would attribute capacity to the wrong country if used as the primary US-02 scan. |
| 115 | US-02 / CORE-03 | Classify owned location storage contribution by rank and capital status | location | `is_capital = yes/no`; `location_rank = location_rank:rural_settlement/town/city/megalopolis` | trigger | Local vanilla `debug/000_johan_debug.txt`, `generic_infrastructure_mission_pack.txt.txt`, `parliament.txt`, `io_parliament.txt`, and building definitions | CONFIRMED | N/A | US-02 applies a non-cumulative priority: capital, megalopolis, city, town, rural settlement. CORE-03 location numerator uses the same local contribution helper. |
| 116 | US-02 | Read country merchant/trade capacity in one market | market with country argument | `scope:<market>.merchant_capacity(<country>)` | value/trigger | Local vanilla `generic_capital_economy_mission_pack.txt` and `market_triggers.txt` | CONFIRMED | N/A | Vanilla mission script stores `scope:mission_target_market.merchant_capacity(root)` in a variable and compares `merchant_capacity = { country = root ... }`. ModeU5 uses the value once per country-market capacity calculation, not once per owned location. |


## Unresolved exposure impact matrix

This matrix maps each `TO_TEST` or `NOT_CONFIRMED` engine exposure to the user stories that reference it directly or inherit its blocked behavior.

| TECH-01 | Unresolved exposure | Impacted US | Blocked behavior | Current fallback / boundary |
|---:|---|---|---|---|
| 021 | Exact target-good syntax and ownership semantics for location output | US-00.1, EPIC US-00, US-00-UI | Aggregate location production into `country × market × good` before stock insertion | Test `goods_output` and `raw_material_output`; no source-level reconstruction is required. |
| 037 | Unmet Pop-relevant need signal by good | US-04, US-04-UI | Detect the shortage or unsatisfied-need signal used for annual demand adaptation and its visibility | Use the US-10.3 unsatisfied Pop-demand outcome when available; otherwise keep the candidate vanilla shortage trigger test-only. |
| 043 | Gameplay-script access to total country wealth | US-05, US-05-UI | Calculate the required `Wealth + Trade Income` ModeU5 Economic Base | Test a script equivalent to `Country.GetTotalWealth`; accept one aggregate only if needed. |
| 044 | Modifiable Stability/Court Economic Base formula hook | US-05, US-05-UI | Replace Tax Base with the ModeU5 Economic Base in the affected vanilla calculations | Direct formula replacement only; reconciliation is out of scope. |
| 056 | Gameplay-script access to actual/desired per-trade quantity | US-10.2, EPIC US-10 | Derive a vanilla transfer request from the actual/desired trade quantity | Test script equivalents to the confirmed GUI accessors; use explicit requested quantity for ModeU5-created transfers. |
| 082 | Read or override RGO dynamic price rules | US-08, US-08-UI | Set RGO construction/upgrade base price to 50 and explain it in UI | Do not override RGO pricing until confirmed. |
| 084 | Exact in-scope building/RGO definition inventory | US-08 | Limit fixed-price overrides to an explicit approved list | Audit local vanilla building and RGO files. |
| 085 | Native price-tooltip binding | US-08-UI | Show the ModeU5 base-price breakdown in vanilla UI | Use documentation/debug until the binding is confirmed. |
| 086 | Estate/other consumer requested quantity | US-10.1, EPIC US-10 | Resolve non-Pop consumption from stock | Require one explicitly accepted simulated-demand caller. |
| 087 | Runtime requested Pop demand quantity by good | US-10.1, EPIC US-10 | Read the requested quantity before resolving it against ModeU5 stock | Use one explicitly accepted simulated-demand source. |
| 088 | Single-entry nested/structured ModeU5 record storage | US-00, US-01, US-02, US-04, US-10.3, US-11 | Replace synchronized physical map families with one scope-valued or genuinely nested record entry | No gameplay behavior is blocked: retain one logical record contract and use centralized helpers over the confirmed map family. |
| 098 | Country/territory lifecycle hook coverage and ordering | CORE-03 | Apply each location capacity-share transfer exactly once and finalize after country lifecycle changes | Use location owner change as the sole proportional split path; delayed country hooks validate/finalize only. |
| 102 | Runtime game-rule gating of arbitrary static numeric overrides | CORE-00, US-07, US-08 | Offer one in-mod toggle that safely restores vanilla building/RGO static values | Use separate optional companion packages; do not load the static overrides in Core. |
| 113 | Numeric precision and rounding behavior | CORE-01, CORE-02, CORE-03, US-03, US-11 | Distinguish a real stock divergence from a fractional assertion, persistence, accumulation, or underflow-guard residue | Keep exact accounting and raw debug values; run the controlled precision probe before proposing any tolerance. |

## Rule

If an entry remains `TO_TEST` or `NOT_CONFIRMED`, gameplay implementation may not depend on it unless the fallback decision is explicitly approved.

When a fallback is accepted, record only one fallback for the PR. Do not implement several competing fallbacks without approval.
