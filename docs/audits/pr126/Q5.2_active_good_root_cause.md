# Q5.2 — PR7.1 active-good root cause and follow-up contract

## Purpose

This checkpoint records the root cause found after reviewing `main` against the PR126 / PR7 dispatcher stack.

The issue is not that PR7 invented an impossible goods count. The issue is that PR7 preserves the existing generated all-good dispatch shape from `main`, while the Q4 performance model and Q5 target language assumed a future active / relevant-good reduction that has not yet been implemented.

## Finding

The game has fewer than the apparent high runtime-good count reported by the PR7 probe. The high counter shape is therefore not a count of distinct goods in EU5. It is a count of generated per-good helper invocations across repeated country / market / phase scopes.

Example interpretation:

```txt
performance_us00_goods = 222
performance_us10_goods = 222

likely shape:
  3 country-present passes
  x 74 generated goods
  = 222 generated helper calls
```

This does not mean there are 222 goods. It means the current dispatcher still schedules `all_goods` helpers once per country-present pass in the detailed market branch.

## Main-branch behavior

`main` already uses generated all-good dispatcher surfaces:

```txt
modeu5_process_us00_monthly_market_all_goods
  -> modeu5_process_us00_monthly_market_good_<each generated good>

modeu5_process_us10_monthly_market_all_goods
  -> modeu5_process_us10_monthly_market_good_<each generated good>
```

The generated wrappers are intentional because per-good map names and helper names must remain literal identifiers in EU5 script.

`main` already contains inner per-good business gates, but those gates happen after the all-good helper has been invoked.

### US-00 main business rule

For each generated good, the US-00 monthly helper should only do full record work when:

```txt
produced_in_market:<good> > 0
OR previous ModeU5 US-00 record / active state exists
```

This is the business rule to preserve in the refactor.

### US-10 main business rule

For each generated good, the US-10 same-market consumption helper should only resolve stock when:

```txt
country has pending same-market consumption request for that market + good
```

Sparse supplier lists then narrow candidate countries after a good is selected for resolution. They are not themselves the market-good scheduler.

## PR7 / #144 behavior

PR7 changes ownership and flow shape, not the generated good-selection model.

The intended PR7 switch is:

```txt
old live path:
  modeu5_run_us00_monthly_pipeline_all_goods
  modeu5_run_monthly_stock_demand_resolution

new live path:
  modeu5_run_monthly_promoted_market_local_cycle
  modeu5_run_monthly_country_trade_owner_cycle
```

Inside the promoted-market local cycle, the current PR7 implementation still calls the generated all-good helpers:

```txt
modeu5_run_promoted_market_live_local_branch_market_all_goods
  -> rebuild countries_present_in_market once
  -> refresh capacity for countries present in market
  -> every country present in market:
       modeu5_process_us00_monthly_market_all_goods
  -> every country present in market:
       modeu5_process_us10_monthly_market_all_goods
```

Therefore, PR7 should be considered an ownership / live-dispatcher switch, not yet the active-good scheduling optimization.

## Root cause classification

```txt
Not introduced by PR7:
  generated all-good US-00 / US-10 helper dispatch already exists on main.

Preserved by PR7:
  PR7 keeps the all-good dispatch inside the new promoted-market local branch.

Missing from PR7:
  explicit active / relevant-good scheduling before invoking per-good helpers.

Documentation mismatch:
  Q4's `G_a` performance model assumes an active-good set, while PR7 currently still uses `G_supported` helper invocation and relies on inner helper gates.
```

## Correct follow-up target

PR7.1 should not invent a new economic rule. It should carry forward the existing `main` rules and make them explicit as pre-dispatch or measured scheduling criteria.

The target design is:

```txt
promoted-market local branch
  -> rebuild countries_present_in_market once
  -> build or measure active/relevant goods for US-00 and US-10
  -> US-00 only schedules goods where:
       produced_in_market:<good> > 0
       OR previous US-00 active state exists
  -> US-10 only schedules goods where:
       pending same-market consumption request exists
  -> sparse supplier lists still narrow candidate countries after US-10 good selection
```

## PR7.1 implementation state

PR7.1 now adds a generated guarded-dispatch layer that follows the repository generator conventions in `tools/README.md` and `docs/technical/GENERATOR_AND_VALIDATOR_MODEL.md`:

```txt
tools/generate_pr71_active_good_dispatch_helpers.sh
tools/templates/modeu5_pr71_active_good_dispatch_good.template.txt
```

`tools/generate_all.sh` runs this generator after the canonical stock-good generator. The generated output target is:

```txt
in_game/common/scripted_effects/modeu5_zz_pr71_active_good_dispatch_generated.txt
```

The `modeu5_zz_..._generated.txt` name intentionally satisfies the ignored generated-file pattern while loading after `modeu5_promoted_market_cycle_effects.txt`, so the PR7.1 same-name override of the live local branch is available after the PR7 base definition.

The generated file contains:

```txt
modeu5_pr71_process_us00_monthly_market_active_goods
modeu5_pr71_process_us10_monthly_market_pending_goods
```

and a PR7.1 override of:

```txt
modeu5_run_promoted_market_live_local_branch_market_all_goods
```

The override preserves the PR7 market-owner branch but replaces the inner calls:

```txt
modeu5_process_us00_monthly_market_all_goods
modeu5_process_us10_monthly_market_all_goods
```

with:

```txt
modeu5_pr71_process_us00_monthly_market_active_goods
modeu5_pr71_process_us10_monthly_market_pending_goods
```

Q4.1 also merges the safe part of the repeated `countries_present_in_market` passes:

```txt
before:
  every present country: refresh capacity
  every present country: run US-00 all generated goods
  every present country: run US-10 all generated goods

after:
  every present country:
    refresh capacity
    run US-00 active-good guarded dispatch
  every present country:
    run US-10 pending-request guarded dispatch
```

US-10 intentionally remains in a second pass. That preserves the Q5.1 ordering rule that all present countries' US-00 production/admission facts are updated before any same-market consumption request scans market stock.

The guards preserve the existing `main` business rules:

```txt
US-00:
  call the heavy per-good helper only when produced_in_market:<good> > 0
  OR previous US-00 state exists

US-10:
  call the heavy per-good helper only when a pending same-market request exists
  for the country + market + good
```

Implementation note: this is still generated literal-symbol code, not a dynamic good list. EU5 script requires literal per-good helper/map identifiers. The improvement is that generated dispatch now performs a light pre-helper gate before entering the heavy generated helper bodies.

## Required metric split

Current PR7 counters can make it look as if the game has more goods than it does. PR7.1 adds generated counters that distinguish considered helper gates from meaningful work:

```txt
US-00:
  modeu5_pr71_us00_goods_considered
  modeu5_pr71_us00_goods_processed
  modeu5_pr71_us00_goods_produced_gate_hits
  modeu5_pr71_us00_goods_previous_state_hits

US-10:
  modeu5_pr71_us10_goods_considered
  modeu5_pr71_us10_requests_processed
  modeu5_pr71_us10_pending_request_hits
```

Definitions:

| Metric | Meaning |
|---|---|
| `*_goods_considered` | Generated light guards evaluated. This can equal countries-present × generated goods. |
| `us00_goods_processed` | Full US-00 helper work actually entered. |
| `us00_goods_produced_gate_hits` | Goods with `produced_in_market:<good> > 0`. |
| `us00_goods_previous_state_hits` | Goods with previous ModeU5 US-00 state requiring cleanup / penalty / ledger continuity. |
| `us10_goods_considered` | Generated US-10 light guards evaluated. |
| `us10_requests_processed` | Pending same-market consumption requests actually resolved. |
| `us10_pending_request_hits` | Goods with pending request map entry for country + market. |

## Validation required

Because PR7.1 changes runtime dispatch, it requires current-head EU5 validation before merge.

Static generation:

```sh
./tools/generate_all.sh
./tools/validate_generators.sh
./tools/validate_modeu5_script_safety.sh
./tools/validate_module_packages.sh
git diff --check
```

Runtime:

```txt
event modeu5_pr126_profile_debug.1
event modeu5_pr126_profile_debug.2
event modeu5_pr126_debug.1
```

Expected shape after the PR7.1 fix:

```txt
modeu5_pr71_us00_goods_considered >= modeu5_pr71_us00_goods_processed
modeu5_pr71_us10_goods_considered >= modeu5_pr71_us10_requests_processed
```

If considered remains high but processed is low, the fix is doing its job: the remaining work is light gate evaluation rather than heavy helper execution.

## PR7.1 acceptance criteria

PR7.1 is acceptable when it proves this state:

```txt
- root cause documented;
- generated PR7.1 guarded dispatcher exists;
- generated dispatcher uses the shared tool library and canonical goods registry;
- repeated per-good generated blocks come from a template under tools/templates/;
- generated output follows the modeu5_*_generated.txt convention;
- generated dispatcher preserves the main US-00 and US-10 business gates;
- generated dispatcher fuses capacity refresh with the US-00 country-present pass;
- generated dispatcher keeps US-10 in a second pass after all present-country US-00 work;
- PR7 market-owner branch uses the guarded dispatcher;
- counters split considered vs processed work;
- runtime proof shows no economic regression and lower meaningful work;
- Q4/Q5 wording does not imply the full `G_a` target until validation confirms it.
```
