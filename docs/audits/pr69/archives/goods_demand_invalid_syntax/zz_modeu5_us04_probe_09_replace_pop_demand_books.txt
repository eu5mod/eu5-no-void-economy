# US-04 injection syntax matrix — candidate 09.
#
# TEST PACKAGE ONLY / DESTRUCTIVE PROBE.
#
# This intentionally replaces the hardcoded pop_demand object for a single-good
# feasibility probe. It must never move to a production package or the normal
# deterministic test package. The goal is to distinguish "additive INJECT cannot
# affect pop_demand" from "pop_demand cannot be modded at all".

REPLACE:pop_demand = {
	books = {
		multiply = global_var:modeu5_us04_q9_replace_global_books
	}
}
