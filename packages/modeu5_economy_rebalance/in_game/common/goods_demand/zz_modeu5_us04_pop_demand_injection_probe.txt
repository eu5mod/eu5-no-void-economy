# US-04 compatibility-first runtime probe, candidate 2.
#
# Candidate 1 used a plain repeated `wheat = { ... }` inside
# `INJECT:pop_demand`; runtime showed that it loaded without parser errors but
# did not affect vanilla wheat demand. Candidate 2 asks the database loader to
# inject recursively into the existing wheat child object.
#
# Nested entry-mode support is not documented for this object shape. Runtime
# validation must establish whether it merges, is ignored, or is rejected.

INJECT:pop_demand = {
	INJECT:wheat = {
		multiply = "modeu5_us04_live_pop_demand_multiplier_wheat"
	}
}
