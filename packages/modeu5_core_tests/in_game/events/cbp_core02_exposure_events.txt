namespace = cbp_core02_probe

cbp_core02_probe.1 = {
	type = country_event
	title = cbp_core02_probe.1.title
	desc = cbp_core02_probe.1.desc
	outcome = neutral
	orphan = yes

	trigger = {
		always = no
	}

	option = {
		name = cbp_core02_probe.delayed.pass
		trigger = {
			has_global_variable = test_cbp_core02_probe_started_after_delay
		}
	}
	option = {
		name = cbp_core02_probe.delayed.fail
		trigger = {
			NOT = {
				has_global_variable = test_cbp_core02_probe_started_after_delay
			}
		}
	}
	option = {
		name = cbp_core02_probe.market.pass
		trigger = {
			has_global_variable = test_cbp_core02_probe_market_found
		}
	}
	option = {
		name = cbp_core02_probe.market.fail
		trigger = {
			NOT = {
				has_global_variable = test_cbp_core02_probe_market_found
			}
		}
	}
	option = {
		name = cbp_core02_probe.value.pass
		trigger = {
			has_global_variable = test_cbp_core02_probe_passed
		}
	}
	option = {
		name = cbp_core02_probe.value.fail
		trigger = {
			NOT = {
				has_global_variable = test_cbp_core02_probe_passed
			}
		}
	}
	option = {
		name = cbp_debug.close
	}
}
