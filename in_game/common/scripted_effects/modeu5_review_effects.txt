modeu5_disable_review_pop = {
	if = {
		limit = {
			has_variable_map = cmm
			is_key_in_variable_map = {
				name = cmm
				target = flag:no_void_economy__review_pop
			}
		}
		remove_from_variable_map = {
			name = cmm
			key = flag:no_void_economy__review_pop
		}
	}
	add_to_variable_map = { name = cmm key = flag:no_void_economy__review_pop value = 0 }
}
