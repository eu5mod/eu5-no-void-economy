namespace = cbp_review

cbp_review.1 = {
	type = country_event
	title = cbp_review.1.title
	desc = cbp_review.1.desc
	historical_info = cbp_review.1.historical_info
	outcome = neutral
	image = "gfx/interface/illustrations/cbp_review/cbp_review_popup_1524_event.dds"

	trigger = {
		is_ai = no
		# current_date >= 1524.2.20
		# OR = {
		# 	NOT = { has_variable_map = cmm }
		# 	AND = {
		# 		has_variable_map = cmm
		# 		OR = {
		# 			NOT = {
		# 				is_key_in_variable_map = {
		# 					name = cmm
		# 					target = flag:no_void_economy__cbp_debug_audit_misc_review_pop_settings
		# 				}
		# 			}
		# 			"variable_map(cmm|flag:no_void_economy__cbp_debug_audit_misc_review_pop_settings)" = 1
		# 		}
		# 	}
		# }
	}

	option = {
		name = cbp_review.1.a
		cbp_disable_review_pop = yes
	}
	option = {
		name = cbp_review.1.b
		cbp_disable_review_pop = yes
	}
}
