namespace = modeu5_review

modeu5_review.1 = {
	type = country_event
	title = modeu5_review.1.title
	desc = modeu5_review.1.desc
	historical_info = modeu5_review.1.historical_info
	outcome = neutral

	trigger = {
		is_ai = no
		current_date >= 1524.2.20
		NOT = { has_variable = modeu5_review_pop_prompt_seen }
		has_variable_map = cmm
		OR = {
			NOT = {
				is_key_in_variable_map = {
					name = cmm
					target = flag:no_void_economy__review_pop
				}
			}
			"variable_map(cmm|flag:no_void_economy__review_pop)" = 1
		}
	}

	immediate = {
		set_variable = modeu5_review_pop_prompt_seen
	}

	option = {
		name = modeu5_review.1.a
		modeu5_disable_review_pop = yes
	}
	option = {
		name = modeu5_review.1.b
		modeu5_disable_review_pop = yes
	}
}
