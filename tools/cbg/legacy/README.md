# Legacy CBG Components

This directory is reserved for deprecated **generic CBG** parsers, adapters, or
migration shims that remain temporarily supported.

Project-specific policy compilers do not belong here. They stay in their host
mod's integration layer, with names such as `generate_<host>_*` or
`validate_<host>_*`.

There are currently no generic legacy components.
