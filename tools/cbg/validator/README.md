# CBG Validators

This directory contains validation for the generic CBG core. Host-specific
integration and parity validators live in a namespaced subdirectory.

Run the generic layout contract from the repository root:

```bash
python3 tools/cbg/validator/validate_cbg_layout.py
```
